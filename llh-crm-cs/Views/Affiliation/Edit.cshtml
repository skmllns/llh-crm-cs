@model llh_crm_cs.Models.Affiliation

@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Edit</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>Affiliation</h4>
        <hr />
        @Html.ValidationSummary(true)
        @Html.HiddenFor(model => model.AffiliationID)

        <div class="form-group">
            @Html.LabelFor(model => model.ConstituentID, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.ConstituentID)
                @Html.ValidationMessageFor(model => model.ConstituentID)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.EmployerName, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.EmployerName)
                @Html.ValidationMessageFor(model => model.EmployerName)
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
