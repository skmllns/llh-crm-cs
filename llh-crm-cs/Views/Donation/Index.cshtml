@model IEnumerable<llh_crm_cs.Models.Donation>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DonorID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DonationArea)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DonationAmount)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DonorID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DonationArea)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DonationAmount)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DonationID }) |
            @Html.ActionLink("Details", "Details", new { id=item.DonationID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DonationID })
        </td>
    </tr>
}

</table>
