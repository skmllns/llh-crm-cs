@model llh_crm_cs.Models.Occurence

@{
    ViewBag.Title = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Create</h2>

@using (Html.BeginForm()) 
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>Occurence</h4>
        <hr />
        @Html.ValidationSummary(true)

        <div class="form-group">
            @Html.Label("Occurence Name", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.OccurenceName)
                @Html.ValidationMessageFor(model => model.OccurenceName)
            </div>
        </div>

        <div class="form-group">
            @Html.Label("Occurence Category", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.OccurenceCategory)
                @Html.ValidationMessageFor(model => model.OccurenceCategory)
            </div>
        </div>

        <div class="form-group">
            @Html.Label("Occurence Date", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.OccurenceDate)
                @Html.ValidationMessageFor(model => model.OccurenceDate)
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
